/**
 * Root layout — required by Next.js App Router.
 * The real html/body/head are rendered inside [lang]/layout.js,
 * so this is intentionally a minimal passthrough.
 */
export default function RootLayout({ children }) {
  return children;
}
